version: "2"

syncs:
  blog-sync:
    src: '.'
    sync_excludes: ['.git/', '.idea/', '.vagrant/', 'var/cache', 'var/log']
    sync_userid: '999'
    sync_strategy: native_osx
    host_disk_mount_mode: 'cached'
    notify_terminal: true
